import { getCurrentAdminProfile } from '@/lib/auth'; import { sql } from '@/lib/neon'; import { redirect } from 'next/navigation'; import { revalidatePath } from 'next/cache'; import { SubmitButton } from '../_components/submit-button'; import { FormFeedback } from '../_components/form-feedback'
async function save(fd: FormData){ 'use server'; try{ const full_name=String(fd.get('full_name')||'').trim(); if(!full_name) redirect('/admin/professionals?error=Verifique+os+campos+obrigat%C3%B3rios'); await sql`insert into professionals (full_name,specialty,cro,schedule_notes,is_active) values (${full_name},${String(fd.get('specialty')||'')||null},${String(fd.get('cro')||'')||null},${String(fd.get('schedule_notes')||'')||null},${fd.get('is_active')==='on'})`; revalidatePath('/admin/professionals'); redirect('/admin/professionals?ok=Profissional+salvo+com+sucesso') }catch(e){ console.error('professionals.save',e); redirect('/admin/professionals?error=N%C3%A3o+foi+poss%C3%ADvel+salvar.+Verifique+os+dados+e+tente+novamente') } }
export default async function Page({searchParams}:{searchParams?:Promise<Record<string,string>>}){ const admin=await getCurrentAdminProfile(); if(!admin) redirect('/admin'); const rows=await sql`select * from professionals order by created_at desc limit 100`; const params=(await searchParams)??{}; return <section className='space-y-4'><h1 className='text-2xl font-bold'>Profissionais</h1><FormFeedback ok={params.ok} error={params.error}/><form action={save} className='grid md:grid-cols-2 gap-3'><input name='full_name' required placeholder='Nome completo *' /><input name='specialty' placeholder='Especialidade' /><input name='cro' placeholder='cro' /><input name='schedule_notes' placeholder='Observações da agenda' /><label className='flex items-center gap-2'><input type='checkbox' name='is_active' defaultChecked />Ativo</label><SubmitButton /></form><div className='overflow-auto'><table className='w-full text-sm'><thead><tr><th>Nome</th><th>Especialidade</th><th>Ativo</th></tr></thead><tbody>{(rows as any[]).map(r=><tr key={r.id}><td>{r.full_name}</td><td>{r.specialty}</td><td>{r.is_active?'Sim':'Não'}</td></tr>)}</tbody></table></div></section> }
